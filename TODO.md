# TODO

Tracking file for ConsultBae AI Automation take-home assignment. Deadline: 2026-08-21, ~10:52 AM IST.

## Setup
- [x] Create public GitHub repo `deaxparadox/consultbae-assignment`, push initial commit (CSVs + .gitignore). CLAUDE.md and docs/claude-web-design/ excluded per user decision.
- [x] ADR 0001: stack & architecture decisions (docs/adr/0001-stack-and-architecture.md)
- [x] Spec + implement Task 1 — Merge pipeline (docs/specs/001-task1-merge-pipeline.md) — 61 people from 102 rows, 41 merges, 1 flagged, 3 malformed rows skipped. See ingest/ingestion_log.txt for the real run output.
- [x] Spec + implement Task 2 — n8n automation (docs/specs/002-task2-n8n-automation.md) — built via browser automation against the real n8n UI; 56/61 people tagged in the live DB, flow exported to n8n/skill-tagging-flow.json.
- [x] Spec + implement Task 3 — Audio collection app (docs/specs/003-task3-audio-app.md) — verified end-to-end via browser automation (upload path), DB reset to clean state (61 people, 0 submissions) after test.
- [x] Spec + implement Task 4 — Data issues report (docs/specs/004-task4-data-issues-report.md) — DATA_ISSUES.md written from real ingestion output + source_records queries; found and fixed a real Task 1 bug in the process.
- [x] Spec + implement Task 5 — Scaling stretch doc (docs/specs/005-task5-scaling-doc.md) — TASK5_SCALING_NOTES.md, grounded in the real ambiguous-match cases found during Task 4.
- [x] Submission package: README.md written (setup steps, data issues/scaling doc links, draft stuck log — **flagged for your review/personalization before submitting**, see README's stuck log note). CHANGELOG and FLOWS.md kept current throughout.
- [x] Docker Compose setup (`docker-compose.yml`, `Dockerfile`, `.dockerignore`) to launch n8n + the audio app together with one command, reusing the existing `n8n_data` volume — verified nothing (owner account, workflow, community node, credentials) was lost switching the running n8n container over to compose.
- [x] Security fix (found by automated commit review): the Docker Compose setup had baked `CLAUDE.md`/`docs/claude-web-design/` into the built image (via `COPY . .` with no `.dockerignore` entry for them) and exposed them live via the runtime bind mounts, undermining the earlier decision to keep them out of the repo; ports also published on `0.0.0.0` instead of localhost, exposing the unauthenticated audio app to the local network. Fixed all three; verified with `docker exec` that both files are unreachable in both containers post-fix, and confirmed n8n's state (login required, workflow, credentials) survived the rebuild.
- [x] Fixed a Windows/Docker Desktop crash in the previous shadow-mount security fix (`error mounting "/dev/null" ... make mountpoint "/data/CLAUDE.md": file exists`) — root cause was mounting the whole repo into n8n/audio_app and then shadow-mounting over specific paths, which is fragile and platform-dependent. Fixed by not mounting the whole repo into those two long-running services at all — each gets only the single file/dir it actually needs (`consultbae.db`, and `audio_uploads/` for the audio app). Moved the full-repo mount (needed to read the CSVs and write the db) to a new one-off `ingest` compose service, gated behind a profile so it never runs as part of `up`. Verified on Linux: `docker compose up` no longer touches `ingest`, both services still work, DB still accessible, n8n state still intact, and `CLAUDE.md`/`docs/claude-web-design` are simply absent (not just empty) in both long-running containers now.
- [x] Fixed `n8n/skill-tagging-flow.json` failing to import in n8n's UI ("The imported data does not contain valid workflow data ('nodes' and 'connections' are missing)") — the `n8n export:workflow` CLI command had wrapped the workflow in a JSON array (`[{...}]`) instead of exporting a plain object, and n8n's UI importer only accepts the plain-object shape. Unwrapped it; verified by test-importing into the live local instance (6 nodes, 5 connections, no error), then deleted the test import without touching the original workflow.
- [ ] Deferred (user call, time pressure before recording): n8n's `Webhook` node in the Skill Auto-Tagging flow has no authentication configured — not externally reachable right now since n8n is bound to `127.0.0.1`, but would let anyone who can reach it trigger real OpenAI calls for free. Also, the OpenAI classification prompt interpolates `skills_raw` with no sanitization and the output is only trimmed, not validated against the 5 known categories — not currently exploitable (`skills_raw` only ever comes from the 2 trusted source CSVs, Task 3 submitters are excluded from this query), but worth hardening before any real exposure. Both flagged by automated commit review, confirmed real, left as-is by explicit user decision.
- [x] Fixed `ingest/ingest.py` crashing with `IsADirectoryError: [Errno 21] Is a directory: '/app/consultbae.db'` when run against a fresh clone on Windows. Cause: `consultbae.db` didn't exist yet, and Docker Desktop creates a placeholder directory (both inside the container and on the host) when a single-file bind mount points at a path that isn't there — triggered by running ingestion via `docker compose run --rm audio_app python ingest/ingest.py` (the pre-narrow-mount command) against `audio_app`'s now-scoped single-file mount, instead of the dedicated `ingest` service. Added a clear, actionable `SystemExit` in the script if `consultbae.db` is ever a directory instead of just letting the raw traceback surface — useful since the user is recording their demo and a cryptic traceback on camera is worse than an explicit message.
- [ ] User: screen recording (≤6 min)
- [ ] User: reply to assignment email with repo + video links

## Notes
- Sequencing is fixed per architecture doc: Task 1 → Task 2 → Task 3 → Task 4 → Task 5.
- Task 2 must be built in n8n's UI (pure-code scores zero) — Claude prepares flow design/queries, user builds and exports the flow JSON.
- Stuck log must be genuinely user-authored (graded specifically on authenticity).
